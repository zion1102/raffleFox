name: raffle_fox
description: "A new Flutter project."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=3.4.4 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  flutter_svg: ^2.0.10+1
  bitsdojo_window: ^0.1.6
  grouped_list: ^6.0.0
  flutter_localizations: 
    sdk: flutter
  cached_network_image: ^3.3.0
  smooth_page_indicator: ^1.2.0+3
  responsive_grid_list: ^1.4.0
  flutter_dash: ^1.0.0
  firebase_core: ^3.3.0 # Lower version
  firebase_analytics: ^11.0.0 # Lower version
  firebase_storage: ^12.0.0 # Lower version
  image_picker: ^1.1.2
  intl: ^0.19.0
  provider: ^6.1.2
  url_launcher: ^6.3.1
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  image: ^4.3.0
  string_similarity: ^2.1.1
  fuzzy: ^0.5.1
  intl_phone_field: ^3.2.0
  country_code_picker: ^3.1.0
  flutter_launcher_icons: ^0.14.1
  cloud_firestore: ^5.3.0 # Lower version
  firebase_auth: ^5.0.0 # Lower version
  firebase_admin: ^0.3.0
  geolocator: ^13.0.2
  geocoding: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0


flutter_icons:
  android: true
  ios: true
  image_path: "assets/icons/logo.jpeg"

flutter: 
  uses-material-design: true
  assets: 
   

    - assets/images/
    - assets/images/Button.svg
    - assets/images/vector.svg
    - assets/images/details.png
    - assets/images/img_group_1000004681.svg
    - assets/images/img_button.svg
    - assets/images/img_vector_1.svg
    - assets/images/img_image.png
    - assets/images/img_group_1000004660.svg
    - assets/images/img_ellipse.svg
    

  
  
